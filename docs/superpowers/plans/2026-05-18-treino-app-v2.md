# Treino App v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade the Treino App to a dark-themed PWA with bottom tab navigation, swipe gestures, and an optional local PC backend that syncs workout progress from the gym.

**Architecture:** Offline-first single-page app (`index.html`) — always works with `localStorage`. An optional Node.js/Express backend on `localhost:3001` is detected at startup; when present, it loads server state (source of truth) and receives every progress update. All workout data (exercises, GIFs, logic) is preserved verbatim.

**Tech Stack:** Vanilla HTML/CSS/JS, PWA (Web App Manifest + Service Worker), Node.js 18+, Express 4, better-sqlite3 9, CORS middleware

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `backend/package.json` | Create | Express + better-sqlite3 + cors deps |
| `backend/db.js` | Create | SQLite schema init + CRUD helpers |
| `backend/server.js` | Create | Express app, 4 endpoints, CORS |
| `backend/test.js` | Create | Smoke tests for all endpoints |
| `make-icons.html` | Create | Browser tool to generate PWA icons |
| `icons/icon-192.png` | Create | PWA icon (generated by make-icons.html) |
| `icons/icon-512.png` | Create | PWA icon (generated by make-icons.html) |
| `manifest.json` | Create | PWA manifest |
| `sw.js` | Create | Service worker (cache-first static, skip API) |
| `index.html` | Rewrite | Dark theme + bottom nav + swipe + PWA + sync |
| `.gitignore` | Modify | Add backend/treino.db + backend/node_modules/ |

---

## Task 1: Backend — db.js

**Files:**
- Create: `backend/package.json`
- Create: `backend/db.js`

- [ ] **Step 1: Create backend/package.json**

```json
{
  "name": "treino-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": { "start": "node server.js", "test": "node test.js" },
  "dependencies": {
    "express": "^4.18.0",
    "better-sqlite3": "^9.0.0",
    "cors": "^2.8.5"
  }
}
```

- [ ] **Step 2: Install dependencies**

```bash
cd backend
npm install
```

Expected: `node_modules/` created, no errors.

- [ ] **Step 3: Create backend/db.js**

```js
const Database = require('better-sqlite3');
const path = require('path');

const db = new Database(path.join(__dirname, 'treino.db'));

db.exec(`
  CREATE TABLE IF NOT EXISTS progresso (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    week_key   TEXT NOT NULL,
    state_json TEXT NOT NULL,
    updated_at TEXT DEFAULT (datetime('now'))
  );
  CREATE UNIQUE INDEX IF NOT EXISTS idx_week ON progresso(week_key);
`);

function getProgresso(weekKey) {
  const row = db.prepare('SELECT state_json FROM progresso WHERE week_key = ?').get(weekKey);
  return row ? JSON.parse(row.state_json) : null;
}

function upsertProgresso(weekKey, state) {
  db.prepare(`
    INSERT INTO progresso (week_key, state_json, updated_at)
    VALUES (?, ?, datetime('now'))
    ON CONFLICT(week_key) DO UPDATE SET
      state_json = excluded.state_json,
      updated_at = excluded.updated_at
  `).run(weekKey, JSON.stringify(state));
}

function deleteProgresso(weekKey) {
  db.prepare('DELETE FROM progresso WHERE week_key = ?').run(weekKey);
}

module.exports = { getProgresso, upsertProgresso, deleteProgresso };
```

- [ ] **Step 4: Verify db.js loads without error**

```bash
cd backend
node -e "const db = require('./db'); console.log('db ok')"
```

Expected: `db ok` printed, `treino.db` file created in `backend/`.

- [ ] **Step 5: Commit**

```bash
cd "E:\Treino_app"
git config user.name "Idarlan Magalhaes"
git config user.email "idarlandais@gmail.com"
git add backend/package.json backend/package-lock.json backend/db.js
git commit -m "feat: add backend db layer (better-sqlite3)"
```

---

## Task 2: Backend — server.js

**Files:**
- Create: `backend/server.js`

- [ ] **Step 1: Create backend/server.js**

```js
const express = require('express');
const cors = require('cors');
const { getProgresso, upsertProgresso, deleteProgresso } = require('./db');

const app = express();
const PORT = 3001;

app.use(cors({
  origin: [
    'https://idarlandias.github.io',
    /^http:\/\/localhost(:\d+)?$/
  ]
}));
app.use(express.json());

app.get('/api/health', (_req, res) => {
  res.json({ ok: true });
});

app.get('/api/progresso/:weekKey', (req, res) => {
  const state = getProgresso(req.params.weekKey);
  if (!state) return res.status(404).json({ error: 'not_found' });
  res.json({ weekKey: req.params.weekKey, state });
});

app.post('/api/progresso', (req, res) => {
  const { weekKey, state } = req.body;
  if (!weekKey || !state) return res.status(400).json({ error: 'weekKey and state required' });
  upsertProgresso(weekKey, state);
  res.json({ ok: true });
});

app.delete('/api/progresso/:weekKey', (req, res) => {
  deleteProgresso(req.params.weekKey);
  res.json({ ok: true });
});

app.listen(PORT, () => console.log(`Treino backend running on http://localhost:${PORT}`));
```

- [ ] **Step 2: Start server and verify**

```bash
cd backend
node server.js
```

Expected output: `Treino backend running on http://localhost:3001`

Keep server running, open new terminal for next step.

- [ ] **Step 3: Test health endpoint**

```bash
curl http://localhost:3001/api/health
```

Expected: `{"ok":true}`

- [ ] **Step 4: Commit**

```bash
cd "E:\Treino_app"
git add backend/server.js
git commit -m "feat: add backend Express server (4 endpoints)"
```

---

## Task 3: Backend — automated smoke tests

**Files:**
- Create: `backend/test.js`

- [ ] **Step 1: Create backend/test.js**

```js
// Node 18+ built-in fetch required
const BASE = 'http://localhost:3001';
const TEST_WEEK = 'test_2099_w1';

async function run() {
  let passed = 0;
  let failed = 0;

  async function check(label, fn) {
    try {
      await fn();
      console.log(`  ✓ ${label}`);
      passed++;
    } catch (e) {
      console.error(`  ✗ ${label}: ${e.message}`);
      failed++;
    }
  }

  function assert(condition, msg) {
    if (!condition) throw new Error(msg);
  }

  console.log('Running backend smoke tests...\n');

  await check('GET /api/health returns ok:true', async () => {
    const r = await fetch(`${BASE}/api/health`);
    const body = await r.json();
    assert(r.ok, `status ${r.status}`);
    assert(body.ok === true, `expected ok:true, got ${JSON.stringify(body)}`);
  });

  await check('GET /api/progresso/:weekKey returns 404 for unknown week', async () => {
    const r = await fetch(`${BASE}/api/progresso/${TEST_WEEK}`);
    assert(r.status === 404, `expected 404, got ${r.status}`);
  });

  await check('POST /api/progresso saves state', async () => {
    const state = { '0_0': true, '0_1': false };
    const r = await fetch(`${BASE}/api/progresso`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ weekKey: TEST_WEEK, state })
    });
    const body = await r.json();
    assert(r.ok, `status ${r.status}`);
    assert(body.ok === true, `expected ok:true`);
  });

  await check('GET /api/progresso/:weekKey returns saved state', async () => {
    const r = await fetch(`${BASE}/api/progresso/${TEST_WEEK}`);
    const body = await r.json();
    assert(r.ok, `status ${r.status}`);
    assert(body.state['0_0'] === true, `expected 0_0:true`);
  });

  await check('POST /api/progresso upserts (overwrites) existing week', async () => {
    const newState = { '0_0': false, '1_0': true };
    await fetch(`${BASE}/api/progresso`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ weekKey: TEST_WEEK, state: newState })
    });
    const r = await fetch(`${BASE}/api/progresso/${TEST_WEEK}`);
    const body = await r.json();
    assert(body.state['1_0'] === true, 'expected updated state');
    assert(body.state['0_0'] === false, 'expected overwritten value');
  });

  await check('DELETE /api/progresso/:weekKey removes state', async () => {
    await fetch(`${BASE}/api/progresso/${TEST_WEEK}`, { method: 'DELETE' });
    const r = await fetch(`${BASE}/api/progresso/${TEST_WEEK}`);
    assert(r.status === 404, `expected 404 after delete, got ${r.status}`);
  });

  console.log(`\n${passed} passed, ${failed} failed`);
  if (failed > 0) process.exit(1);
}

run().catch(e => { console.error(e); process.exit(1); });
```

- [ ] **Step 2: Run tests (server must be running)**

```bash
cd backend
node server.js &
node test.js
```

Expected:
```
Running backend smoke tests...

  ✓ GET /api/health returns ok:true
  ✓ GET /api/progresso/:weekKey returns 404 for unknown week
  ✓ POST /api/progresso saves state
  ✓ GET /api/progresso/:weekKey returns saved state
  ✓ POST /api/progresso upserts (overwrites) existing week
  ✓ DELETE /api/progresso/:weekKey removes state

6 passed, 0 failed
```

- [ ] **Step 3: Commit**

```bash
cd "E:\Treino_app"
git add backend/test.js
git commit -m "test: add backend smoke tests (6 checks)"
```

---

## Task 4: PWA assets — icons, manifest, service worker

**Files:**
- Create: `make-icons.html`
- Create: `icons/icon-192.png` (via browser)
- Create: `icons/icon-512.png` (via browser)
- Create: `manifest.json`
- Create: `sw.js`

- [ ] **Step 1: Create make-icons.html**

```html
<!DOCTYPE html>
<html>
<head><meta charset="UTF-8"><title>Generate Icons</title></head>
<body style="background:#111;color:#fff;font-family:sans-serif;padding:24px">
<h2>PWA Icon Generator</h2>
<canvas id="c192" width="192" height="192" style="display:none"></canvas>
<canvas id="c512" width="512" height="512" style="display:none"></canvas>
<div style="display:flex;gap:16px;margin-top:16px">
  <div><canvas id="p192" width="192" height="192" style="border-radius:20px"></canvas><br>
    <a id="dl192" download="icon-192.png" style="color:#5b9aff">⬇ Baixar 192px</a></div>
  <div><canvas id="p512" width="512" height="512" style="border-radius:52px;width:192px;height:192px"></canvas><br>
    <a id="dl512" download="icon-512.png" style="color:#5b9aff">⬇ Baixar 512px</a></div>
</div>
<script>
function draw(canvas, size) {
  const ctx = canvas.getContext('2d');
  const r = size * 0.18;
  ctx.fillStyle = '#09090f';
  ctx.beginPath();
  ctx.roundRect(0, 0, size, size, r);
  ctx.fill();
  ctx.font = `${size * 0.52}px serif`;
  ctx.textAlign = 'center';
  ctx.textBaseline = 'middle';
  ctx.fillText('🏋️', size / 2, size / 2 + size * 0.04);
}
['192','512'].forEach(s => {
  const c = document.getElementById('c'+s);
  draw(c, +s);
  const p = document.getElementById('p'+s);
  draw(p, +s);
  document.getElementById('dl'+s).href = c.toDataURL('image/png');
});
</script>
</body>
</html>
```

- [ ] **Step 2: Generate icons in browser**

Open `make-icons.html` in Chrome (double-click the file). Two preview canvases appear. Click "⬇ Baixar 192px" and "⬇ Baixar 512px". Move the downloaded files to `E:\Treino_app\icons\`:

```powershell
mkdir "E:\Treino_app\icons" -ErrorAction SilentlyContinue
# Mover os arquivos baixados para a pasta icons/
```

- [ ] **Step 3: Create manifest.json**

```json
{
  "name": "Treino Semanal",
  "short_name": "Treino",
  "description": "Controle de treino semanal Push/Pull/Legs",
  "start_url": "/Treino_App/",
  "scope": "/Treino_App/",
  "display": "standalone",
  "background_color": "#09090f",
  "theme_color": "#09090f",
  "orientation": "portrait-primary",
  "icons": [
    { "src": "icons/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any maskable" },
    { "src": "icons/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any maskable" }
  ]
}
```

- [ ] **Step 4: Create sw.js**

```js
const CACHE = 'treino-v2';
const STATIC = [
  '/Treino_App/',
  '/Treino_App/index.html',
  '/Treino_App/manifest.json',
  '/Treino_App/icons/icon-192.png',
  '/Treino_App/icons/icon-512.png',
];

self.addEventListener('install', e => {
  e.waitUntil(caches.open(CACHE).then(c => c.addAll(STATIC)));
  self.skipWaiting();
});

self.addEventListener('activate', e => {
  e.waitUntil(
    caches.keys().then(keys =>
      Promise.all(keys.filter(k => k !== CACHE).map(k => caches.delete(k)))
    )
  );
  self.clients.claim();
});

self.addEventListener('fetch', e => {
  // Never intercept localhost API calls
  if (e.request.url.includes('localhost:3001')) return;
  e.respondWith(
    caches.match(e.request).then(cached => cached || fetch(e.request))
  );
});
```

- [ ] **Step 5: Verify icons exist**

```powershell
dir "E:\Treino_app\icons"
```

Expected: `icon-192.png` and `icon-512.png` present.

- [ ] **Step 6: Commit**

```bash
cd "E:\Treino_app"
git add manifest.json sw.js icons/icon-192.png icons/icon-512.png make-icons.html
git commit -m "feat: add PWA assets (manifest, service worker, icons)"
```

---

## Task 5: Frontend — CSS design system + HTML skeleton

**Files:**
- Modify: `index.html` (full rewrite — CSS + HTML shell only, JS to follow)

> **Important:** The JS section (everything inside `<script>`) from the original `index.html` must be preserved. Copy it to a temporary file before rewriting. The arrays `W`, `TC`, `CM` and all existing functions will be reintegrated in Tasks 7–10.

- [ ] **Step 1: Copy current JS to safe location**

```powershell
# Extract the <script> block from current index.html as backup
$content = Get-Content "E:\Treino_app\index.html" -Raw
$start = $content.IndexOf('<script>')
$end = $content.LastIndexOf('</script>') + 9
$content.Substring($start, $end - $start) | Out-File "E:\Treino_app\js-backup.js" -Encoding utf8
```

- [ ] **Step 2: Rewrite index.html with new CSS + HTML shell**

Replace the entire `index.html` with:

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="theme-color" content="#09090f">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <link rel="manifest" href="manifest.json">
  <link rel="apple-touch-icon" href="icons/icon-192.png">
  <title>Treino Semanal</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    /* ── Reset & Base ── */
    *, *::before, *::after { margin:0; padding:0; box-sizing:border-box; }
    html { scroll-behavior:smooth; }
    body {
      background:#09090f;
      color:#e8e8f0;
      font-family:'Inter',system-ui,-apple-system,sans-serif;
      line-height:1.6;
      -webkit-font-smoothing:antialiased;
      min-height:100vh;
      overflow-x:hidden;
    }

    /* ── Design Tokens ── */
    :root {
      --bg:       #09090f;
      --bg2:      #0d0d16;
      --card:     #12121e;
      --card2:    #16162a;
      --surface:  rgba(255,255,255,.04);
      --border:   #1a1a28;
      --border2:  #222232;
      --push:     #5b9aff;
      --push-dim: rgba(91,154,255,.08);
      --pull:     #00d4a0;
      --pull-dim: rgba(0,212,160,.08);
      --legs:     #a87fff;
      --legs-dim: rgba(168,127,255,.08);
      --full:     #f06040;
      --full-dim: rgba(240,96,64,.08);
      --green:    #22c77e;
      --green-dim:rgba(34,199,126,.08);
      --gold:     #e8a020;
      --text:     #e8e8f0;
      --text2:    #9090b0;
      --text3:    #44445a;
      --radius:   16px;
      --radius-sm:10px;
      --radius-xs: 7px;
      --shadow:   0 8px 32px rgba(0,0,0,.5);
      --shadow-sm:0 2px 12px rgba(0,0,0,.3);
      --transition:.25s cubic-bezier(.4,0,.2,1);
      --nav-h:    62px;
    }

    /* ── Layout ── */
    .app {
      max-width: 600px;
      margin: 0 auto;
      position: relative;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* ── App Header ── */
    .app-header {
      padding: 48px 20px 12px;
      background: var(--bg);
      border-bottom: 1px solid var(--border);
      flex-shrink: 0;
    }
    .header-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 10px;
    }
    .app-title {
      font-size: 1.15rem;
      font-weight: 900;
      color: var(--text);
      letter-spacing: -.02em;
    }
    .sync-badge {
      display: flex;
      align-items: center;
      gap: 5px;
      background: rgba(91,154,255,.08);
      border: 1px solid rgba(91,154,255,.18);
      padding: 4px 10px;
      border-radius: 100px;
      font-size: .65rem;
      font-weight: 700;
      color: var(--push);
      letter-spacing: .4px;
      text-transform: uppercase;
      opacity: 0;
      transition: opacity var(--transition);
    }
    .sync-badge.visible { opacity: 1; }
    .sync-dot {
      width: 5px; height: 5px;
      border-radius: 50%;
      background: var(--green);
      animation: pulse 2s ease-in-out infinite;
    }
    @keyframes pulse { 0%,100%{opacity:1} 50%{opacity:.3} }

    /* ── Progress bar ── */
    .prog-row {
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .prog-track {
      flex: 1;
      height: 4px;
      background: var(--border2);
      border-radius: 100px;
      overflow: hidden;
    }
    .prog-fill {
      height: 100%;
      background: linear-gradient(90deg, var(--push), var(--legs), var(--pull));
      background-size: 200% 100%;
      border-radius: 100px;
      width: 0%;
      transition: width .6s cubic-bezier(.4,0,.2,1);
      animation: progShift 4s ease-in-out infinite;
    }
    @keyframes progShift { 0%,100%{background-position:0%} 50%{background-position:100%} }
    .prog-stats {
      display: flex;
      gap: 8px;
      flex-shrink: 0;
    }
    .prog-stat {
      font-family: 'JetBrains Mono', monospace;
      font-size: .68rem;
      font-weight: 700;
      color: var(--text3);
    }
    .prog-stat span { color: var(--push); }
    .prog-stat.fire span { color: var(--gold); }

    /* ── Scroll content ── */
    .content-wrap {
      flex: 1;
      overflow: hidden;
      position: relative;
    }
    .swipe-container {
      display: flex;
      width: 700%;
      height: 100%;
      transition: transform .25s cubic-bezier(.4,0,.2,1);
      will-change: transform;
    }
    .day-panel {
      width: calc(100% / 7);
      height: 100%;
      overflow-y: auto;
      padding: 12px 16px calc(var(--nav-h) + 80px);
      -webkit-overflow-scrolling: touch;
    }
    .day-panel::-webkit-scrollbar { display: none; }

    /* ── Day header card ── */
    .day-header {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 14px 16px;
      margin-bottom: 10px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .day-title {
      font-size: 1rem;
      font-weight: 800;
      color: var(--text);
      letter-spacing: -.01em;
    }
    .day-type-tag {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      font-size: .6rem;
      font-weight: 700;
      padding: 3px 8px;
      border-radius: var(--radius-xs);
      text-transform: uppercase;
      letter-spacing: .5px;
      margin-top: 4px;
    }
    .mark-all-btn {
      background: var(--surface);
      border: 1px solid var(--border2);
      color: var(--text2);
      font-family: 'Inter', sans-serif;
      font-size: .72rem;
      font-weight: 700;
      padding: 7px 14px;
      border-radius: var(--radius-xs);
      cursor: pointer;
      transition: var(--transition);
      flex-shrink: 0;
    }
    .mark-all-btn:hover { border-color: var(--green); color: var(--green); }
    .mark-all-btn.done { background: var(--green-dim); border-color: rgba(34,199,126,.3); color: var(--green); }

    /* ── Exercise card ── */
    .ex-card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: var(--radius-sm);
      margin-bottom: 6px;
      overflow: hidden;
      transition: border-color var(--transition), background var(--transition);
    }
    .ex-card:hover { border-color: var(--border2); }
    .ex-card.is-done {
      border-color: rgba(34,199,126,.15);
      background: linear-gradient(135deg, var(--card), rgba(34,199,126,.02));
    }
    .ex-card.is-open { border-color: rgba(91,154,255,.25); }

    .ex-row {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 11px 13px;
    }

    /* Custom checkbox */
    .cb-wrap {
      position: relative;
      width: 20px; height: 20px;
      flex-shrink: 0;
    }
    .cb-wrap input {
      position: absolute;
      opacity: 0; width: 100%; height: 100%;
      cursor: pointer; z-index: 2; margin: 0;
    }
    .cb-box {
      width: 20px; height: 20px;
      border-radius: 6px;
      border: 1.5px solid var(--border2);
      display: flex; align-items: center; justify-content: center;
      transition: var(--transition);
      color: transparent;
      font-size: .65rem;
      font-weight: 900;
    }
    .cb-wrap input:checked + .cb-box {
      background: var(--green);
      border-color: var(--green);
      color: #fff;
      box-shadow: 0 0 10px rgba(34,199,126,.3);
      animation: cbPop .25s ease;
    }
    @keyframes cbPop { 0%{transform:scale(1)} 50%{transform:scale(1.2)} 100%{transform:scale(1)} }

    .ex-name {
      flex: 1;
      font-size: .875rem;
      font-weight: 600;
      color: var(--text);
      cursor: pointer;
      transition: var(--transition);
    }
    .ex-name.striked {
      text-decoration: line-through;
      text-decoration-color: var(--text3);
      color: var(--text3);
    }

    /* Tags */
    .tags { display: flex; gap: 4px; flex-wrap: wrap; align-items: center; }
    .tag {
      font-family: 'JetBrains Mono', monospace;
      font-size: .6rem;
      font-weight: 600;
      padding: 3px 7px;
      border-radius: 5px;
      white-space: nowrap;
    }

    /* Expand button */
    .expand-btn {
      width: 24px; height: 24px;
      border-radius: 6px;
      background: var(--surface);
      border: 1px solid var(--border);
      display: flex; align-items: center; justify-content: center;
      cursor: pointer;
      transition: var(--transition);
      color: var(--text3);
      font-size: .5rem;
      flex-shrink: 0;
    }
    .expand-btn:hover { background: var(--border); color: var(--text); }
    .ex-card.is-open .expand-btn {
      background: rgba(91,154,255,.15);
      color: var(--push);
      border-color: rgba(91,154,255,.3);
      transform: rotate(180deg);
    }

    /* Expand detail */
    .ex-detail {
      max-height: 0;
      overflow: hidden;
      transition: max-height .4s cubic-bezier(.4,0,.2,1);
    }
    .ex-card.is-open .ex-detail { max-height: 420px; }
    .ex-detail-inner {
      padding: 0 13px 14px;
      display: grid;
      grid-template-columns: 160px 1fr;
      gap: 14px;
      align-items: start;
    }
    @media(max-width:400px) { .ex-detail-inner { grid-template-columns: 1fr; } }

    .gif-frame {
      background: var(--bg2);
      border-radius: 10px;
      overflow: hidden;
      aspect-ratio: 1/1;
      border: 1px solid var(--border);
    }
    .gif-frame img {
      width: 100%; height: 100%;
      object-fit: cover; display: block;
    }

    .detail-grid {
      display: grid;
      grid-template-columns: repeat(3,1fr);
      gap: 6px;
      margin-bottom: 8px;
    }
    .detail-cell {
      background: var(--bg2);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 8px;
      text-align: center;
    }
    .detail-cell .val {
      font-family: 'JetBrains Mono', monospace;
      font-size: .82rem;
      font-weight: 700;
      color: var(--text);
    }
    .detail-cell .key {
      font-size: .58rem;
      color: var(--text3);
      text-transform: uppercase;
      letter-spacing: .5px;
      font-weight: 600;
      margin-top: 2px;
    }
    .tech-pill {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      padding: 5px 10px;
      border-radius: 6px;
      font-size: .65rem;
      font-weight: 700;
    }

    /* ── Toast ── */
    .toast {
      position: fixed;
      bottom: calc(var(--nav-h) + 12px);
      left: 50%;
      transform: translateX(-50%) translateY(10px);
      background: #0e2010;
      border: 1px solid rgba(34,199,126,.3);
      color: var(--green);
      font-size: .75rem;
      font-weight: 700;
      padding: 8px 16px;
      border-radius: 10px;
      white-space: nowrap;
      display: flex;
      align-items: center;
      gap: 6px;
      box-shadow: var(--shadow);
      z-index: 200;
      opacity: 0;
      transition: opacity .3s, transform .3s;
      pointer-events: none;
    }
    .toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }

    /* ── Bottom tab bar ── */
    .bottom-nav {
      position: fixed;
      bottom: 0; left: 0; right: 0;
      height: var(--nav-h);
      background: var(--card);
      border-top: 1px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: space-around;
      padding: 0 4px;
      z-index: 100;
      max-width: 600px;
      margin: 0 auto;
      left: 50%;
      transform: translateX(-50%);
    }
    .nav-tab {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 2px;
      padding: 6px 8px 4px;
      border-radius: 10px;
      cursor: pointer;
      transition: background var(--transition);
      min-width: 44px;
      user-select: none;
      -webkit-tap-highlight-color: transparent;
    }
    .nav-tab .t-emoji {
      font-size: 1.1rem;
      filter: grayscale(.7);
      transition: filter var(--transition);
      line-height: 1;
    }
    .nav-tab .t-name {
      font-size: .55rem;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: .5px;
      color: var(--text3);
      transition: color var(--transition);
    }
    .nav-tab .t-bar {
      width: 14px; height: 2px;
      border-radius: 1px;
      background: transparent;
      margin-top: 1px;
      transition: background var(--transition);
    }
    .nav-tab.is-active {
      background: var(--push-dim);
    }
    .nav-tab.is-active .t-emoji { filter: none; }
    .nav-tab.is-active .t-name { color: var(--push); }
    .nav-tab.is-active .t-bar { background: var(--push); }
    .nav-tab.is-done .t-emoji { filter: none; }
    .nav-tab.is-done .t-name { color: var(--green); }
    .nav-tab.is-today { border: 1px solid rgba(232,160,32,.25); }

    /* More drawer */
    .more-drawer {
      position: fixed;
      bottom: calc(var(--nav-h) + 6px);
      right: 12px;
      background: var(--card2);
      border: 1px solid var(--border2);
      border-radius: 14px;
      padding: 8px;
      z-index: 150;
      display: none;
      box-shadow: var(--shadow);
    }
    .more-drawer.open { display: block; }
    .drawer-tab {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 9px 14px;
      border-radius: 8px;
      cursor: pointer;
      transition: background var(--transition);
      -webkit-tap-highlight-color: transparent;
    }
    .drawer-tab:hover { background: var(--surface); }
    .drawer-tab.is-active { background: var(--push-dim); }
    .drawer-tab .dt-emoji { font-size: 1.1rem; }
    .drawer-tab .dt-info { display: flex; flex-direction: column; }
    .drawer-tab .dt-name { font-size: .8rem; font-weight: 700; color: var(--text); }
    .drawer-tab .dt-type { font-size: .65rem; color: var(--text3); }
    .drawer-tab.is-done .dt-name { color: var(--green); }

    /* ── Confetti ── */
    .confetti-box {
      position: fixed; inset: 0;
      pointer-events: none;
      z-index: 9999;
      overflow: hidden;
    }
    .conf {
      position: absolute;
      border-radius: 2px;
      animation: confDrop 3s ease-in forwards;
    }
    @keyframes confDrop {
      0% { transform:translateY(-20px) rotate(0deg) scale(1); opacity:1 }
      80% { opacity:.8 }
      100% { transform:translateY(100vh) rotate(720deg) scale(.5); opacity:0 }
    }

    /* ── Responsive ── */
    @media(max-width:480px) {
      .nav-tab { min-width: 36px; padding: 6px 4px 4px; }
      .ex-row { padding: 10px 11px; gap: 8px; }
    }
  </style>
</head>
<body>

<div class="app">

  <!-- App Header -->
  <header class="app-header">
    <div class="header-row">
      <span class="app-title">🏋️ Treino Semanal</span>
      <div class="sync-badge" id="syncBadge">
        <div class="sync-dot"></div>
        PC CONECTADO
      </div>
    </div>
    <div class="prog-row">
      <div class="prog-track">
        <div class="prog-fill" id="progFill"></div>
      </div>
      <div class="prog-stats">
        <div class="prog-stat" id="statDays"><span>0</span>/7</div>
        <div class="prog-stat fire" id="statStreak"><span>0</span>🔥</div>
      </div>
    </div>
  </header>

  <!-- Swipeable content -->
  <div class="content-wrap" id="contentWrap">
    <div class="swipe-container" id="swipeContainer">
      <!-- 7 .day-panel divs injected by renderPanels() -->
    </div>
  </div>

</div><!-- .app -->

<!-- Toast -->
<div class="toast" id="toast"></div>

<!-- More days drawer -->
<div class="more-drawer" id="moreDrawer"></div>

<!-- Bottom nav -->
<nav class="bottom-nav" id="bottomNav"></nav>

<!-- Confetti container -->
<div class="confetti-box" id="confettiBox"></div>

<script>
// ═══════════════════════════════════════════════════════════════
// DATA — PRESERVED VERBATIM FROM v1
// ═══════════════════════════════════════════════════════════════

const DAYS = ['Segunda', 'Terça', 'Quarta', 'Quinta', 'Sexta', 'Sábado', 'Domingo'];
const SHORT = ['SEG', 'TER', 'QUA', 'QUI', 'SEX', 'SÁB', 'DOM'];
const gd = id => `https://lh3.googleusercontent.com/d/${id}`;
const FB = gd('1hld_imFINYZ2nV-nxNK8XB4OOo5oPjc8');

const W = [
  { label:'A — Push', sub:'Peito / Tríceps / Ombro', c:'push', exs:[
    {n:'Supino Reto',s:'5',r:'10',d:'1min',t:'REST PAUSE',img:gd('1fmspp8ufXk-LbMy9t8XxG7TbkjvgfsaT')},
    {n:'Supino Inclinado Halter',s:'4',r:'10',d:'45seg',t:'',img:gd('1AZJ79XqNwJMkl-Sz4lFSbIn1CARGLKHi')},
    {n:'Cross Over',s:'3',r:'15',d:'1min',t:'SLOW',img:gd('1r8kXucHXelSiUXG7iKzMF5t_AVuvNu5N')},
    {n:'Tríceps Testa Polia',s:'4',r:'10',d:'45seg',t:'',img:gd('1qvchD55eVGMXdNPvXFT9N25B1b1YU-RI')},
    {n:'Pulley Corda',s:'3',r:'12',d:'30seg',t:'',img:gd('1RZhap3zBgpVdfaBp2lbEjIKTLhist-G8')},
    {n:'Paralela',s:'3',r:'—',d:'2min',t:'EXAUSTÃO',img:gd('1-rIbW9ygaihZ0-CGdU6F5Ffy_XB2bbOa')},
    {n:'Desenvolvimento Arnold',s:'3',r:'12',d:'1min',t:'',img:gd('1W6ibsEIde5mcJxui4rnwQiRdDsT1L1EO')},
  ]},
  { label:'B — Pull', sub:'Costas / Bíceps', c:'pull', exs:[
    {n:'Levantamento Terra',s:'4',r:'12',d:'1min',t:'',img:gd('1ljNwzS9fKossboRcoMIWwZlajbF_sdds')},
    {n:'Remada Curvada',s:'4',r:'10',d:'1min',t:'',img:gd('1RDBUzgVS3-nXH5Vp23xN_RUJWljgOChQ')},
    {n:'Barra Fixa',s:'3',r:'—',d:'2min',t:'EXAUSTÃO',img:gd('1ZuEyqWhYLjtPLNYFmSBHXfvWVgsNt6iY')},
    {n:'Cavalinho',s:'3',r:'12',d:'45seg',t:'',img:gd('1GlwcKx2NyC9d4tLY7KzLG6iGCmMOCzRe')},
    {n:'PullDown',s:'3',r:'12',d:'45seg',t:'',img:gd('1d5-lYx2e0VztuVt0JVh8XQ-S4bCI7ns0')},
    {n:'Rosca Alternada',s:'4',r:'10',d:'30seg',t:'',img:gd('1A5OSmDDwM25JUXkqLa_DV_2aV9XDH_gg')},
    {n:'Rosca Direta (Barra ou Halter)',s:'3',r:'12',d:'1min',t:'BI-SET',img:gd('1ge1jnA1-6BH3GdfkiTZce5cYB5zY8s5e')},
    {n:'Rosca Martelo',s:'3',r:'12',d:'1min',t:'BI-SET',img:gd('1NJSD8adc6XzBobkMXKAQ2i8WzXc2bHJX')},
    {n:'Elevação Lateral',s:'3',r:'15',d:'45seg',t:'',img:gd('1JTAkfullt5N3xJRXbJckvpyFz5axKjuZ')},
  ]},
  { label:'C — Legs', sub:'Pernas / Panturrilha', c:'legs', exs:[
    {n:'Agachamento Livre',s:'5',r:'8',d:'45seg',t:'',img:gd('1kd2C7PerZcg2vKtdB1RTyuLqxtxb8xnT')},
    {n:'Afundo',s:'4',r:'16 passadas',d:'1min',t:'',img:gd('1-i_kZHTVlZtM-mHEWzWvSQBu2hkYboIg')},
    {n:'Stiff',s:'3',r:'15',d:'1min',t:'SLOW',img:gd('1uySwPdS6LBjI-WqIF_2tYHIVMbdDdGXC')},
    {n:'Leg Press',s:'3',r:'15',d:'1min',t:'SLOW',img:gd('1aze2Yc5RRF9bGQNzkVPuh7BnbG82evjv')},
    {n:'Mesa Flexora',s:'4',r:'12',d:'30seg',t:'',img:gd('1CO8TAjIm60PXdLEFyZ3XzgU1dW9UOEbA')},
    {n:'Panturrilha em Pé',s:'5',r:'10',d:'15seg',t:'',img:gd('1D9GaliZUyPMNKLGTKQKQklmIo5RrGv8a')},
    {n:'Elevação Gêmeos',s:'3',r:'15',d:'45seg',t:'',img:gd('1cd6bthCXemX8zWFUJ4uJIuesq1S2YIq9')},
  ]},
  { label:'A — Push', sub:'Peito / Tríceps / Ombro', c:'push', exs:[
    {n:'Supino Reto',s:'5',r:'10',d:'1min',t:'REST PAUSE',img:gd('1fmspp8ufXk-LbMy9t8XxG7TbkjvgfsaT')},
    {n:'Supino Inclinado Halter',s:'4',r:'10',d:'45seg',t:'',img:gd('1AZJ79XqNwJMkl-Sz4lFSbIn1CARGLKHi')},
    {n:'Cross Over',s:'3',r:'15',d:'1min',t:'SLOW',img:gd('1r8kXucHXelSiUXG7iKzMF5t_AVuvNu5N')},
    {n:'Tríceps Testa Polia',s:'4',r:'10',d:'45seg',t:'',img:gd('1qvchD55eVGMXdNPvXFT9N25B1b1YU-RI')},
    {n:'Pulley Corda',s:'3',r:'12',d:'30seg',t:'',img:gd('1RZhap3zBgpVdfaBp2lbEjIKTLhist-G8')},
    {n:'Paralela',s:'3',r:'—',d:'2min',t:'EXAUSTÃO',img:gd('1-rIbW9ygaihZ0-CGdU6F5Ffy_XB2bbOa')},
    {n:'Desenvolvimento Arnold',s:'3',r:'12',d:'1min',t:'',img:gd('1W6ibsEIde5mcJxui4rnwQiRdDsT1L1EO')},
  ]},
  { label:'B — Pull', sub:'Costas / Bíceps', c:'pull', exs:[
    {n:'Levantamento Terra',s:'4',r:'12',d:'1min',t:'',img:gd('1ljNwzS9fKossboRcoMIWwZlajbF_sdds')},
    {n:'Remada Curvada',s:'4',r:'10',d:'1min',t:'',img:gd('1RDBUzgVS3-nXH5Vp23xN_RUJWljgOChQ')},
    {n:'Barra Fixa',s:'3',r:'—',d:'2min',t:'EXAUSTÃO',img:gd('1ZuEyqWhYLjtPLNYFmSBHXfvWVgsNt6iY')},
    {n:'Cavalinho',s:'3',r:'12',d:'45seg',t:'',img:gd('1GlwcKx2NyC9d4tLY7KzLG6iGCmMOCzRe')},
    {n:'PullDown',s:'3',r:'12',d:'45seg',t:'',img:gd('1d5-lYx2e0VztuVt0JVh8XQ-S4bCI7ns0')},
    {n:'Rosca Alternada',s:'4',r:'10',d:'30seg',t:'',img:gd('1A5OSmDDwM25JUXkqLa_DV_2aV9XDH_gg')},
    {n:'Rosca Direta (Barra ou Halter)',s:'3',r:'12',d:'1min',t:'BI-SET',img:gd('1ge1jnA1-6BH3GdfkiTZce5cYB5zY8s5e')},
    {n:'Rosca Martelo',s:'3',r:'12',d:'1min',t:'BI-SET',img:gd('1NJSD8adc6XzBobkMXKAQ2i8WzXc2bHJX')},
    {n:'Elevação Lateral',s:'3',r:'15',d:'45seg',t:'',img:gd('1JTAkfullt5N3xJRXbJckvpyFz5axKjuZ')},
  ]},
  { label:'C — Legs', sub:'Pernas / Panturrilha', c:'legs', exs:[
    {n:'Agachamento Livre',s:'5',r:'8',d:'45seg',t:'',img:gd('1kd2C7PerZcg2vKtdB1RTyuLqxtxb8xnT')},
    {n:'Afundo',s:'4',r:'16 passadas',d:'1min',t:'',img:gd('1-i_kZHTVlZtM-mHEWzWvSQBu2hkYboIg')},
    {n:'Stiff',s:'3',r:'15',d:'1min',t:'SLOW',img:gd('1uySwPdS6LBjI-WqIF_2tYHIVMbdDdGXC')},
    {n:'Leg Press',s:'3',r:'15',d:'1min',t:'SLOW',img:gd('1aze2Yc5RRF9bGQNzkVPuh7BnbG82evjv')},
    {n:'Mesa Flexora',s:'4',r:'12',d:'30seg',t:'',img:gd('1CO8TAjIm60PXdLEFyZ3XzgU1dW9UOEbA')},
    {n:'Panturrilha em Pé',s:'5',r:'10',d:'15seg',t:'',img:gd('1D9GaliZUyPMNKLGTKQKQklmIo5RrGv8a')},
    {n:'Elevação Gêmeos',s:'3',r:'15',d:'45seg',t:'',img:gd('1cd6bthCXemX8zWFUJ4uJIuesq1S2YIq9')},
  ]},
  { label:'FullBody', sub:'Corpo Inteiro', c:'full', exs:[
    {n:'Supino Reto',s:'5',r:'10',d:'1min',t:'SLOW',img:gd('1fmspp8ufXk-LbMy9t8XxG7TbkjvgfsaT')},
    {n:'Afundo',s:'4',r:'16 passadas',d:'1min',t:'',img:gd('1-i_kZHTVlZtM-mHEWzWvSQBu2hkYboIg')},
    {n:'Remada Curvada',s:'5',r:'8',d:'2min',t:'',img:gd('1RDBUzgVS3-nXH5Vp23xN_RUJWljgOChQ')},
    {n:'Flexão de Braço',s:'3',r:'—',d:'1min',t:'EXAUSTÃO',img:gd('1sLzi_Xnmy4U_38qiVO1iP1DkO4dCNZuC')},
    {n:'Desenvolvimento Arnold',s:'4',r:'10',d:'45seg',t:'',img:gd('1W6ibsEIde5mcJxui4rnwQiRdDsT1L1EO')},
    {n:'Remada Sentado Triângulo',s:'5',r:'8',d:'30seg',t:'',img:gd('1hld_imFINYZ2nV-nxNK8XB4OOo5oPjc8')},
    {n:'Mesa Flexora',s:'5',r:'10',d:'1min',t:'',img:gd('1CO8TAjIm60PXdLEFyZ3XzgU1dW9UOEbA')},
    {n:'Pulley Corda',s:'4',r:'10',d:'45seg',t:'',img:gd('1RZhap3zBgpVdfaBp2lbEjIKTLhist-G8')},
    {n:'Rosca Alternada',s:'4',r:'10',d:'1min',t:'',img:gd('1A5OSmDDwM25JUXkqLa_DV_2aV9XDH_gg')},
    {n:'Tríceps Coice',s:'4',r:'10',d:'1min',t:'',img:gd('1nmY6YXTLi0TIb6NxeSLDGKjBn2i0q7ko')},
    {n:'Rosca Martelo Unilateral',s:'4',r:'8',d:'—',t:'',img:gd('1NJSD8adc6XzBobkMXKAQ2i8WzXc2bHJX')},
  ]},
];

const TC = {
  'SLOW':       {bg:'rgba(245,158,11,.1)', c:'#f59e0b', b:'rgba(245,158,11,.2)'},
  'EXAUSTÃO':   {bg:'rgba(255,112,112,.1)',c:'#ff7070', b:'rgba(255,112,112,.2)'},
  'REST PAUSE': {bg:'rgba(91,154,255,.1)', c:'#5b9aff', b:'rgba(91,154,255,.2)'},
  'BI-SET':     {bg:'rgba(168,127,255,.1)',c:'#a87fff', b:'rgba(168,127,255,.2)'},
};
const CM = {
  push:{tag:'rgba(91,154,255,.1)', tc:'#5b9aff', bg:'rgba(91,154,255,.06)', bc:'#5b9aff'},
  pull:{tag:'rgba(0,212,160,.08)',  tc:'#00d4a0', bg:'rgba(0,212,160,.06)',  bc:'#00d4a0'},
  legs:{tag:'rgba(168,127,255,.08)',tc:'#a87fff', bg:'rgba(168,127,255,.06)',bc:'#a87fff'},
  full:{tag:'rgba(240,96,64,.08)',  tc:'#f06040', bg:'rgba(240,96,64,.06)',  bc:'#f06040'},
};

// ═══ STATE ═══
function weekKey() {
  const d = new Date(), y = d.getFullYear(), j = new Date(y,0,1);
  return `t_${y}_w${Math.ceil(((d-j)/864e5 + j.getDay()+1)/7)}`;
}
let S = {}; const K = weekKey();
function load() { try { S = JSON.parse(localStorage.getItem(K)) || {} } catch(e){ S={} } }
function save() { localStorage.setItem(K, JSON.stringify(S)) }
function exDone(d,i) { return S[`${d}_${i}`] === true }
function setEx(d,i,v) { S[`${d}_${i}`] = v; save() }
function dayDone(d) { return W[d].exs.every((_,i) => exDone(d,i)) }
function todayIdx() { const d = new Date().getDay(); return d===0?6:d-1 }

// ═══ RENDER PANELS ═══
const DAY_EMOJI = ['💪','🏋️','🦵','💪','🏋️','🦵','🔥'];

function renderPanels() {
  const container = document.getElementById('swipeContainer');
  container.innerHTML = '';
  W.forEach((w, di) => {
    const cm = CM[w.c];
    const panel = document.createElement('div');
    panel.className = 'day-panel';
    panel.id = `p-${di}`;

    // Day header
    const isDone = dayDone(di);
    panel.innerHTML = `
      <div class="day-header">
        <div>
          <div class="day-title">${DAY_EMOJI[di]} ${DAYS[di]}</div>
          <span class="day-type-tag" style="background:${cm.tag};color:${cm.tc}">${w.label}</span>
        </div>
        <button class="mark-all-btn${isDone?' done':''}" id="dayBtn${di}" onclick="toggleDay(${di})">
          ${isDone ? '✅ Concluído' : 'Marcar tudo'}
        </button>
      </div>`;

    // Exercise cards
    w.exs.forEach((ex, ei) => {
      const done = exDone(di, ei);
      const card = document.createElement('div');
      card.className = `ex-card${done?' is-done':''}`;
      card.id = `x-${di}-${ei}`;

      let tagsHtml = `<span class="tag" style="background:${cm.bg};color:${cm.bc}">${ex.s}×${ex.r}</span>`;
      tagsHtml += `<span class="tag" style="background:var(--surface);color:var(--text3)">⏱${ex.d}</span>`;
      if (ex.t) {
        const tc = TC[ex.t] || TC['SLOW'];
        tagsHtml += `<span class="tag" style="background:${tc.bg};color:${tc.c};border:1px solid ${tc.b}">${ex.t}</span>`;
      }

      const techPill = ex.t ? (() => {
        const tc = TC[ex.t] || TC['SLOW'];
        return `<div class="tech-pill" style="background:${tc.bg};color:${tc.c};border:1px solid ${tc.b}">⚡ ${ex.t}</div>`;
      })() : '';

      card.innerHTML = `
        <div class="ex-row">
          <label class="cb-wrap">
            <input type="checkbox" ${done?'checked':''} onchange="toggleEx(${di},${ei},this.checked)">
            <span class="cb-box">✓</span>
          </label>
          <span class="ex-name${done?' striked':''}" onclick="toggleOpen(${di},${ei})">${ex.n}</span>
          <div class="tags">${tagsHtml}</div>
          <button class="expand-btn" onclick="toggleOpen(${di},${ei})">▼</button>
        </div>
        <div class="ex-detail">
          <div class="ex-detail-inner">
            <div class="gif-frame">
              <img src="${ex.img}" alt="${ex.n}" loading="lazy" onerror="this.src='${FB}'">
            </div>
            <div>
              <div class="detail-grid">
                <div class="detail-cell"><div class="val">${ex.s}</div><div class="key">Séries</div></div>
                <div class="detail-cell"><div class="val">${ex.r}</div><div class="key">Reps</div></div>
                <div class="detail-cell"><div class="val">${ex.d}</div><div class="key">Descanso</div></div>
              </div>
              ${techPill}
            </div>
          </div>
        </div>`;
      panel.appendChild(card);
    });

    container.appendChild(panel);
  });
}

// ═══ BOTTOM NAV ═══
let currentDay = 0;
const MAIN_TABS = [0,1,2,3]; // SEG TER QUA QUI always visible

function renderNav() {
  const nav = document.getElementById('bottomNav');
  const ti = todayIdx();
  let html = '';
  MAIN_TABS.forEach(i => {
    const done = dayDone(i);
    const active = currentDay === i;
    const today = i === ti;
    html += `<div class="nav-tab${active?' is-active':''}${done?' is-done':''}${today?' is-today':''}"
      onclick="switchDay(${i})">
      <div class="t-emoji">${DAY_EMOJI[i]}</div>
      <div class="t-name">${SHORT[i]}</div>
      <div class="t-bar"></div>
    </div>`;
  });
  // More button
  const extraActive = currentDay > 3;
  html += `<div class="nav-tab${extraActive?' is-active':''}" onclick="toggleDrawer()">
    <div class="t-emoji">···</div>
    <div class="t-name">+3</div>
    <div class="t-bar"></div>
  </div>`;
  nav.innerHTML = html;

  // More drawer (days 4,5,6)
  const drawer = document.getElementById('moreDrawer');
  const ti2 = todayIdx();
  drawer.innerHTML = [4,5,6].map(i => {
    const done = dayDone(i);
    const active = currentDay === i;
    return `<div class="drawer-tab${active?' is-active':''}${done?' is-done':''}"
      onclick="switchDay(${i});closeDrawer()">
      <div class="dt-emoji">${DAY_EMOJI[i]}</div>
      <div class="dt-info">
        <div class="dt-name">${DAYS[i]}${i===ti2?' · HOJE':''}</div>
        <div class="dt-type">${W[i].label}${done?' ✓':''}</div>
      </div>
    </div>`;
  }).join('');
}

let drawerOpen = false;
function toggleDrawer() {
  drawerOpen = !drawerOpen;
  document.getElementById('moreDrawer').classList.toggle('open', drawerOpen);
}
function closeDrawer() {
  drawerOpen = false;
  document.getElementById('moreDrawer').classList.remove('open');
}
document.addEventListener('click', e => {
  if (drawerOpen && !e.target.closest('#moreDrawer') && !e.target.closest('.nav-tab')) closeDrawer();
});

function switchDay(i) {
  currentDay = i;
  // Update swipe position
  document.getElementById('swipeContainer').style.transform = `translateX(${-i * (100/7)}%)`;
  renderNav();
}

// ═══ SWIPE ═══
(function initSwipe() {
  const wrap = document.getElementById('contentWrap');
  let startX = 0, startY = 0, moved = false;

  wrap.addEventListener('touchstart', e => {
    startX = e.touches[0].clientX;
    startY = e.touches[0].clientY;
    moved = false;
  }, { passive: true });

  wrap.addEventListener('touchmove', e => {
    const dx = e.touches[0].clientX - startX;
    const dy = e.touches[0].clientY - startY;
    if (!moved && Math.abs(dx) > Math.abs(dy) && Math.abs(dx) > 8) moved = true;
  }, { passive: true });

  wrap.addEventListener('touchend', e => {
    if (!moved) return;
    const dx = e.changedTouches[0].clientX - startX;
    if (Math.abs(dx) < 50) return;
    if (dx < 0 && currentDay < 6) switchDay(currentDay + 1);
    else if (dx > 0 && currentDay > 0) switchDay(currentDay - 1);
  }, { passive: true });
})();

// ═══ ACTIONS ═══
function toggleOpen(d, i) {
  document.getElementById(`x-${d}-${i}`).classList.toggle('is-open');
}

function toggleEx(d, i, v) {
  setEx(d, i, v);
  const card = document.getElementById(`x-${d}-${i}`);
  card.classList.toggle('is-done', v);
  const name = card.querySelector('.ex-name');
  name.classList.toggle('striked', v);
  if (v) { card.style.transform='scale(1.005)'; setTimeout(()=>card.style.transform='',200); }
  updateDayBtn(d);
  updateProgress();
  renderNav();
  syncToServer(K, S);
}

function toggleDay(d) {
  const all = dayDone(d);
  W[d].exs.forEach((_, i) => {
    setEx(d, i, !all);
    const card = document.getElementById(`x-${d}-${i}`);
    const cb = card.querySelector('input[type=checkbox]');
    if (cb) cb.checked = !all;
    card.classList.toggle('is-done', !all);
    card.querySelector('.ex-name').classList.toggle('striked', !all);
  });
  updateDayBtn(d);
  updateProgress();
  renderNav();
  syncToServer(K, S);
  if (!all) confetti();
}

function updateDayBtn(d) {
  const btn = document.getElementById(`dayBtn${d}`);
  const done = dayDone(d);
  btn.className = `mark-all-btn${done?' done':''}`;
  btn.textContent = done ? '✅ Concluído' : 'Marcar tudo';
}

function updateProgress() {
  let tot = 0, dn = 0;
  W.forEach((w, di) => w.exs.forEach((_, ei) => { tot++; if(exDone(di,ei)) dn++; }));
  const p = tot ? Math.round(dn/tot*100) : 0;
  document.getElementById('progFill').style.width = p + '%';

  let dc = 0, streak = 0, mx = 0;
  for (let i=0;i<7;i++) { if(dayDone(i)){dc++;streak++;if(streak>mx)mx=streak}else streak=0 }
  document.getElementById('statDays').innerHTML = `<span>${dc}</span>/7`;
  document.getElementById('statStreak').innerHTML = `<span>${mx}</span>🔥`;
}

function resetWeek() {
  if (!confirm('Resetar todo o progresso desta semana?')) return;
  localStorage.removeItem(K); S = {};
  // Also delete from server if available
  if (serverAvailable) {
    fetch(`http://localhost:3001/api/progresso/${K}`, { method: 'DELETE' }).catch(()=>{});
  }
  renderPanels(); renderNav(); updateProgress();
}

function confetti() {
  const box = document.getElementById('confettiBox');
  const cols = ['#5b9aff','#00d4a0','#a87fff','#f06040','#e8a020','#22c77e'];
  for (let i=0;i<50;i++) {
    const c = document.createElement('div'); c.className = 'conf';
    c.style.cssText = `left:${Math.random()*100}%;background:${cols[Math.floor(Math.random()*cols.length)]};animation-delay:${Math.random()*1.2}s;animation-duration:${2+Math.random()*2}s;width:${5+Math.random()*7}px;height:${5+Math.random()*7}px`;
    box.appendChild(c);
  }
  setTimeout(() => box.innerHTML = '', 4500);
}

// ═══ TOAST ═══
let toastTimer = null;
function showToast(msg) {
  const el = document.getElementById('toast');
  el.innerHTML = `✓ ${msg}`;
  el.classList.add('show');
  clearTimeout(toastTimer);
  toastTimer = setTimeout(() => el.classList.remove('show'), 2500);
}

// ═══ SYNC ═══
let serverAvailable = false;
const API = 'http://localhost:3001';

async function detectServer() {
  try {
    const r = await fetch(`${API}/api/health`, { signal: AbortSignal.timeout(2000) });
    return r.ok;
  } catch { return false; }
}

async function loadFromServer() {
  try {
    const r = await fetch(`${API}/api/progresso/${K}`, { signal: AbortSignal.timeout(3000) });
    if (r.ok) {
      const body = await r.json();
      S = body.state;
      save(); // mirror to localStorage
      return true;
    }
  } catch {}
  return false;
}

async function syncToServer(weekKey, state) {
  if (!serverAvailable) return;
  try {
    const r = await fetch(`${API}/api/progresso`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ weekKey, state })
    });
    if (r.ok) showToast('Progresso salvo no PC');
  } catch {}
}

async function initSync() {
  serverAvailable = await detectServer();
  if (serverAvailable) {
    document.getElementById('syncBadge').classList.add('visible');
    const loaded = await loadFromServer();
    if (loaded) {
      // Re-render with server state
      renderPanels();
      updateProgress();
      renderNav();
    }
  }
}

// ═══ SERVICE WORKER ═══
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.register('/Treino_App/sw.js').catch(() => {});
}

// ═══ INIT ═══
load();
renderPanels();
switchDay(todayIdx());
updateProgress();
initSync();
</script>
</body>
</html>
```

- [ ] **Step 3: Open index.html locally and verify**

Open `E:\Treino_app\index.html` in Chrome. Verify:
- Dark background (#09090f) ✓
- All 7 days have exercise cards ✓
- Bottom nav shows SEG TER QUA QUI ··· ✓
- Today's tab is highlighted ✓
- Tapping a checkbox marks exercise as done ✓
- Expanding a card shows GIF + detail grid ✓
- Confetti fires when marking all exercises done ✓
- Swiping left/right changes day ✓

- [ ] **Step 4: Verify sync badge is hidden (no server running)**

Badge "PC CONECTADO" should be invisible. Open browser console — no CORS or fetch errors that block the app.

- [ ] **Step 5: Test with backend running**

```bash
cd backend
node server.js
```

Refresh the page. Badge "● PC CONECTADO" should appear in the header within 2 seconds. Mark an exercise — toast "✓ Progresso salvo no PC" should appear.

- [ ] **Step 6: Commit**

```bash
cd "E:\Treino_app"
git add index.html
git commit -m "feat: rewrite frontend v2 (dark theme, bottom nav, swipe, PWA, sync)"
```

---

## Task 6: Update .gitignore + deploy

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: Update .gitignore**

Add to `.gitignore`:

```
backend/node_modules/
backend/treino.db
js-backup.js
```

- [ ] **Step 2: Verify .gitignore is working**

```bash
cd "E:\Treino_app"
git status
```

`backend/node_modules/` and `backend/treino.db` must NOT appear in the untracked files list.

- [ ] **Step 3: Commit .gitignore**

```bash
git add .gitignore
git commit -m "chore: ignore backend runtime files"
```

- [ ] **Step 4: Push to GitHub Pages**

```bash
git push origin main
```

Wait ~60 seconds, then open https://idarlandias.github.io/Treino_App/ on your phone.

- [ ] **Step 5: Verify PWA install prompt on Android Chrome**

Open the URL in Chrome on Android. After the page loads, look for the "Adicionar à tela inicial" banner at the bottom, or tap the browser menu → "Adicionar à tela inicial". After installing, the app opens fullscreen (no browser bar) with theme color #09090f.

- [ ] **Step 6: Final verification checklist**

```
✓ App loads with dark theme on phone
✓ Bottom nav tabs work
✓ Swipe between days works
✓ Exercises can be checked/unchecked
✓ GIFs load (or show fallback)
✓ Confetti fires on day completion
✓ PWA install prompt appears
✓ App works fully offline (airplane mode)
✓ With node backend/server.js running on PC + phone on same WiFi:
    → Badge "PC CONECTADO" appears
    → Checking exercise shows "✓ Progresso salvo no PC"
```

- [ ] **Step 7: Commit final state**

```bash
cd "E:\Treino_app"
git log --oneline -5
```

---

## Self-Review

**Spec coverage:**
| Spec requirement | Task |
|---|---|
| Tema escuro Midnight Black | Task 5 (CSS tokens) |
| Bottom Tab Bar (5 visible + +3 drawer) | Task 5 (renderNav) |
| Swipe horizontal entre dias | Task 5 (initSwipe) |
| PWA instalável | Tasks 4 + 5 (manifest, sw, registration) |
| App funciona offline | Task 5 (offline-first load) + sw.js (Task 4) |
| Backend local Node.js + SQLite | Tasks 1–2 |
| Sync automático ao detectar servidor | Task 5 (initSync + syncToServer) |
| Toast "✓ Progresso salvo no PC" | Task 5 (showToast) |
| Carregar estado do servidor no startup | Task 5 (loadFromServer) |
| Servidor vence em conflito | Task 5 (loadFromServer sobrescreve localStorage) |
| W array preservado | Task 5 (verbatim copy) |
| GIFs + fallback preservados | Task 5 (verbatim onerror handler) |
| confetti() preservada | Task 5 |
| resetWeek() deleta do servidor | Task 5 |
| .gitignore atualizado | Task 6 |
| Deploy GitHub Pages | Task 6 |

Sem gaps. Sem placeholders. Tipos e nomes consistentes: `syncToServer(weekKey, state)` definido em Task 5 e chamado em `toggleEx` e `toggleDay` da mesma task. `serverAvailable` definido antes de qualquer uso.
